# 필수 — 없으면 layout.tsx metadataBase에서 500
NEXT_PUBLIC_APP_URL=http://localhost:3000

# OPENAI_API_KEY는 이 파일이 아니라 "사용자 홈의 .env" 또는 OS 환경변수에 둔다.
# (src/app/api/generate/route.ts 참조 — 저장소에 키가 들어올 여지를 없애기 위함)
